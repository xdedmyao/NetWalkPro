# “This folder contains experimental / deprecated code”
# crawler_ports.py
This is a script for crawling the data (port_code, port_name(English), port_name(Chinese)) for all ports.
The data generated are stored in 34 csv files in "data/ports"
